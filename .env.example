# Supabase — copy this file to .env.local and fill in from your Supabase
# project's Settings -> API. .env.local is git-ignored; never commit real values.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
